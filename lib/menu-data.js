// Source unique des données par défaut (partagée entre le fallback runtime et le seed).
import data from '../data/menu.json'

export const CATEGORIES = data.categories
export const PRODUCTS = data.products
export const DEFAULT_CONFIG = data.config
